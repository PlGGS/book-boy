/// @description  ib_book_destroy(book instance id)
/// @param book instance id
//
// Destroys the book and its data structures and deletes its vertex buffers and normal buffers.
//
// book instance id: The instance id of the book. Returned at book creation.

with (argument0) instance_destroy();
